# Java Cache Concurrent HashMap

## Maven

    mvn archetype:generate -DgroupId=com.pro -DartifactId=java-cache-concurrenthashmap -DarchetypeArtifactId=maven-archetype-quickstart -DinteractiveMode=false
    
    mvn clean package


